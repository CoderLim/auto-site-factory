{
  "name": "@factory/worker",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "once": "npm run build && node dist/index.js --once",
    "targets:sync": "npm run build && node dist/sync-targets.js"
  },
  "dependencies": {
    "@factory/database": "0.1.0",
    "@factory/discovery": "0.1.0",
    "@factory/shared": "0.1.0",
    "an-array-of-english-words": "^2.0.0"
  }
}
